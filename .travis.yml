language: cpp

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - g++-5
            - gcc-5

compiler:
    - gcc

script: 
  - cmake . && make -j64 

after_script:
    - ctest
